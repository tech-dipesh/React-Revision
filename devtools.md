# React DevTools:
  1. Componenet:
    - It shows two props and the hooks what have we used
    1. Hooks:
      - on the hooks it doesn't show the: `useState` rather it shows only: `state`
     - We Also can change the devtools value from the hooks

    2. Props:
     - for practice we also can pass the new props
     - 
  - Parse: show the error on the individual comps

  ## Above menu:
  - Inspect the matching dom: it'll redirect to the element tag of that comps dom
  - Log this compoennet: I'tll show the data of that comps on the console tab, shows: 1. Props, 2. Hoods, 3. Nodes, 4. Location
  - View source for this element: Open a jsx file on the sources
  - Suspend the selected element:we'll learn later.
  - ### Also have the inspect dom element like that devtools have.

2. Profile:
  It'll record the all componenet record. 
  - for recording first start recording and refresh and do some operation after stop recording it'll show the result
  1. Flamegraph chart: shows much time it take to load.
  2. Ranked Chart:shows ranking wise
  
  Also show the why it cause too much time to render.

  For making optimized our applicatioon we use the Profile

