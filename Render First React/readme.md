# npx is the temporary command where it install a temporarya a parcel and runs it.
# while on the other hand npm first install a that package and only runs after that


# the vite build is convert from jsx to js by: If you’re using Vite, esbuild does this for you.


# we've to start a any react component name with the capital letter due to not conflict with the htmll element.


